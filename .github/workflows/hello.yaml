name: hello world
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello world"
