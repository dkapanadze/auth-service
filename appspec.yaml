version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/nestjs-api
permissions:
  - object: /home/ubuntu/nestsj-api
    pattern: '**'
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    timeout: 300
    runas: ubuntu
